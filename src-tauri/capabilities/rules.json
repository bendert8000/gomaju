{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "rules",
  "description": "Capability for the break-rules quick-select window. Empty permission set: it only invokes app-defined commands (cmd_get_rules / cmd_set_rule_flags / cmd_close_rules / cmd_open_settings / cmd_window_ready), which are not capability-gated. Those commands are restricted to this window by the require_rules() caller-check in src/commands.rs.",
  "windows": ["rules"],
  "permissions": []
}
